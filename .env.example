# Argus Environment Configuration
# Copy to .env and fill in your values: cp .env.example .env

# LLM Provider (openai, anthropic, gemini)
ARGUS_LLM_PROVIDER=openai
ARGUS_LLM_API_KEY=sk-your-api-key-here
ARGUS_LLM_MODEL=gpt-4o

# Host IP/domain (used for CORS and Docker networking)
ARGUS_HOST=localhost

# Optional: Custom CORS origins (comma-separated)
# ARGUS_CORS_ORIGINS=http://myhost:3000,https://mydomain.com
